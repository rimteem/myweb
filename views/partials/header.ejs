<!DOCTYPE html>
<html lang="<%= currentLocale %>" dir="<%= currentLocale === 'ar' ? 'rtl' : 'ltr' %>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= __('siteTitle') %></title>
    <link rel="stylesheet" href="/css/style.css">
    <% if (currentLocale === 'ar') { %>
        <link href="https://fonts.googleapis.com/css2?family=Noto+Kufi+Arabic:wght@400;500;600;700&display=swap" rel="stylesheet">
    <% } else { %>
        <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
    <% } %>
</head>
<body>
    <header class="site-header">
        <div class="container header-container">
            <a href="<%= getLocalizedPath('/') %>" class="logo">Rim<span>token</span></a>
            <div class="header-right-content">
                <nav class="main-nav">
                    <ul>
                        <li><a href="<%= getLocalizedPath('/') %>#about" class="active"><%= __('navAbout') %></a></li>
                        <li><a href="<%= getLocalizedPath('/') %>#features"><%= __('navFeatures') %></a></li>
                        <li><a href="<%= getLocalizedPath('/') %>#team"><%= __('navTeam') %></a></li>
                        <li><a href="<%= getLocalizedPath('/') %>#market"><%= __('navMarket') %></a></li>
                    </ul>
                </nav>
                <div class="auth-and-lang-selector">
                    <div class="header-auth-icons">
                        <a href="<%= getLocalizedPath('/login') %>" title="<%= __('login') %>">
                            <img src="/images/login-icon-placeholder.png" alt="<%= __('login') %>" class="auth-icon"> <!-- Replace with actual icon -->
                            <span><%= __('login') %></span>
                        </a>
                        <a href="<%= getLocalizedPath('/register') %>" title="<%= __('register') %>">
                            <img src="/images/register-icon-placeholder.png" alt="<%= __('register') %>" class="auth-icon"> <!-- Replace with actual icon -->
                            <span><%= __('register') %></span>
                        </a>
                    </div>
                    <div class="language-selector">
                        <a href="?lang=en" class="lang-icon <%= currentLocale === 'en' ? 'active-lang' : '' %>" title="English"><%= __('langEN') %></a>
                        <a href="?lang=ar" class="lang-icon <%= currentLocale === 'ar' ? 'active-lang' : '' %>" title="العربية"><%= __('langAR') %></a>
                        <a href="?lang=zh" class="lang-icon <%= currentLocale === 'zh' ? 'active-lang' : '' %>" title="中文"><%= __('langZH') %></a>
                        <a href="?lang=fr" class="lang-icon <%= currentLocale === 'fr' ? 'active-lang' : '' %>" title="Français"><%= __('langFR') %></a>
                    </div>
                </div>
            </div>
             <button class="mobile-nav-toggle" aria-label="Toggle navigation">☰</button>
        </div>
    </header>
    <main>